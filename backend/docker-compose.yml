services:
  redis:
    image: redis:alpine
    ports:
      - "6379:6379"
    restart: always

  honeypot:
    build: .
    ports:
      - "5000:5000"
    env_file:
      - .env
    environment:
      - REDIS_URL=redis://redis:6379
    depends_on:
      - redis
    restart: always
